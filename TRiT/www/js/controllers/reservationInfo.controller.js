angular.module('app.controllers')
.controller('reservationInfoCtrl', ['$scope', '$stateParams',
function ($scope, $stateParams) {
  // 넘어온 데이터 바인딩 (+고유번호 포함)
  // QR코드 생성
  // Home 버튼 클릭 이벤트 (mainCtrl로 이동)
}])
