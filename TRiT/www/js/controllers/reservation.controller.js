angular.module('app.controllers')
.controller('reservationCtrl', ['$scope', '$stateParams',
function ($scope, $stateParams) {
  // partnerDetailCtrl 에서 가져온 데이터 바인딩
  // Cancel 버튼 클릭 이벤트 (뒤로 이동)
  // Payment Detail 버튼 클릭 이벤트 (가지고 있는 데이터 PaymentDetailsCtrl로 전달)
}]);
